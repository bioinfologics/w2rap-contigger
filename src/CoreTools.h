#ifndef CORE_TOOLS_H
#define CORE_TOOLS_H

// CoreTools.h contains includes for files having nothing per se to do
// with assembly or even sequences, but which are needed by almost all
// modules.  The list of includes is deliberately sparse, because most
// things should not be needed by almost all modules.

#include "system/System.h"
#include "system/Assert.h"
#include "system/Types.h"
#include "Vec.h"
#include "dvString.h"
#include "VecString.h"

#endif
